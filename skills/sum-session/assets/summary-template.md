# 开发总结报告

## 1. 背景（Background）

### 业务背景
{说明本次任务发生的业务场景：是什么系统、什么模块、面对什么用户群体}

### 触发原因
{说明为什么要做这件事：是生产环境出了事故？用户投诉？技术债务？新功能需求？}

### 不解决的后果
{如果本次不改，会导致什么问题继续存在或恶化？}

---

## 2. 发现过程（Discovery Process）

### 触发信号
{问题最初是怎么被发现的？监控告警？用户反馈？Code Review？性能分析？}

### 排查路径
{从现象到根因的排查链路，每一步的判断依据和排除过程}

```
现象：{最初的表象}
  ↓
初步判断：{第一反应是什么原因}
  ↓
排除：{排除了哪些可能性，为什么排除}
  ↓
定位：{最终确认的根因位置}
```

### 关键证据
{附上关键的错误日志片段、监控截图、数据对比等}

---

## 3. 问题说明（Problem Statement）

### 直接原因
{技术层面的近因：哪行代码、什么条件下触发了问题}

### 根本原因
{更深层的根因：设计缺陷、边界条件遗漏、第三方依赖变更等}

### 影响范围
- 影响功能：{哪些功能受到了影响}
- 影响用户：{影响了多少用户 / 什么用户群体}
- 影响时长：{问题持续了多长时间（如果是线上事故）}
- 数据影响：{是否有数据损坏或丢失（如果有）}

---

## 4. 解决方案（Solution）

### 采用的方案
{用 2-3 段话说明最终采用了什么方案，在哪些文件做了什么样的修改}

### 方案选择理由
{为什么选这个方案而不是其他方案}

### 考虑过但未采用的方案

| 方案 | 优点 | 缺点 | 不选的原因 |
|------|------|------|-----------|
| 方案 A | ... | ... | ... |
| 方案 B | ... | ... | ... |

### 关键改动清单
- `path/to/file1.ts` — {做了什么改动，为什么}
- `path/to/file2.ts` — {做了什么改动，为什么}

---

## 5. 最终效果（Outcome）

### 测试结果
{引用 EXAMINE 阶段的测试结果}
- 测试总数：N
- 通过：N
- 失败：0

### 验收标准达成情况
- [x] {验收标准 1} — 验证方式：{如何验证的}
- [x] {验收标准 2} — 验证方式：{如何验证的}

### 量化效果（如适用）
| 指标 | 修改前 | 修改后 | 变化 |
|------|--------|--------|------|
| {指标 1} | {数值} | {数值} | {变化} |

---

## 6. 未来展望（Future Outlook）

### 遗留问题
- {本次未能解决但已知的问题 1}
- {本次未能解决但已知的问题 2}

### 技术债务
- {本次因时间/资源限制而采用的临时方案，需后续改进}

### 后续建议
1. {具体的后续行动建议 1 — 如：上线后观察 XX 指标 1 周}
2. {具体的后续行动建议 2 — 如：下个迭代将临时补丁改为正式方案}
3. {具体的后续行动建议 3 — 如：补充 XX 场景的自动化测试}

### 关联任务
- {如果需要创建后续 task 或关联其他项目，在此记录}
