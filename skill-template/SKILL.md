---
name: your-skill-name
description: >
  简要描述这个技能做什么（一句话）。Agent 会根据这个描述判断何时加载。
  写描述时站在 Agent 的角度："我有什么问题时应该加载这个 skill？"
allowed-tools: Read, Write, Bash, Search, Grep, Glob  # 根据实际需要删减
model: sonnet  # haiku / sonnet / opus — 该技能推荐的模型 tier
version: 1.0
---

# {技能名称（中文）}

## 前置判断

**必须执行**：{什么情况下必须执行这个步骤}

**可以跳过**：{什么情况下可以跳过}

## 核心流程

### 子步骤 1：{名称}

{详细的操作指令。使用命令式语气："检查…"、"确保…"、"搜索…"}

### 子步骤 2：{名称}

{详细的操作指令}

> **详细参考资料**：{细节内容、常见陷阱的检查清单，见 `references/{name}.md`。}

## Gotchas（踩坑记录）

> **重要**：这是本技能信号密度最高的内容。记录特定于本领域的反直觉陷阱。
> 每当 Agent 或人类在本技能执行中踩坑后，在此追加一条记录。
> 详细格式见 `references/guide.md` 中的 Gotchas 编写指南。

```markdown
### {陷阱标题（10 字以内）}

- **触发条件**：什么情况下会触发这个问题
- **错误表现**：触发后会看到什么
- **根因**：为什么会这样
- **正确做法**：应该怎么做
- **首次发现**：YYYY-MM-DD — {场景}
```

## 反合理化防御

> Agent 在执行本技能时可能产生以下自我欺骗。每次执行前先过一遍。

| Agent 可能对自己说的借口 | 为什么这是错的 |
|------------------------|-------------|
| {借口1} | {反驳1} |
| {借口2} | {反驳2} |

## 遇到{XX异常}时

如果{触发条件}，停止并报告：

```
【{技能名称} 阻断 - {异常类型}】
问题：{具体描述}
选项：
  A) {方案A + 利弊}
  B) {方案B + 利弊}
  C) {其他}
→ 请选择。
```

## 测试提示词

> 修改本技能后，用以下提示词验证是否仍然正确工作。

### 基础测试

```prompt
{一个能触发本技能的最小提示词}
```

**预期行为**：
- {行为1}
- {行为2}

**通过标准**：
- [ ] {标准1}
- [ ] {标准2}

### 边界测试

```prompt
{一个测试边界情况的提示词——触发条件接近但应触发或不应触发}
```

**预期行为**：
- {行为1}

## 反模式

- ❌ {禁止行为1}
- ❌ {禁止行为2}
- ❌ {禁止行为3}

---

## Before/After 对比

### Before（没有本技能时）
```
{Agent 的典型错误行为——无约束时的输出示例}
```

### After（有本技能后）
```
{Agent 的正确行为——遵循本技能后的输出示例}
```
